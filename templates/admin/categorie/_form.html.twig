{{ form_start(form, {'attr': {'class': 'container mt-3', id: 'new-cat-form'}}) }}
    {{ form_errors(form) }}
    
    {{ form_row(form.nom, {'attr': {'class': 'form-control'}}) }}
{{ form_end(form) }}
